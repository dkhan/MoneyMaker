<h1>Partners</h1>

<p><%= link_to 'New Partner', new_partner_path %></p>

<table>
  <tr>
    <th>Nickname</th>
    <th>Email</th>
  </tr>

  <% @partners.each do |partner| %>
    <tr>
      <td><%= partner.nickname %></td>
      <td><%= partner.email %></td>
    </tr>
  <% end %>
</table>